{
  "name": "iris-rocm",
  "image": "gfx1250-ffm:latest",
  "runArgs": [
    "--network=host",
    "--device=/dev/kfd",
    "--device=/dev/dri",
    "--group-add=video",
    "--ipc=host",
    "--cap-add=SYS_PTRACE",
    "--security-opt=seccomp=unconfined"
  ],
  "workspaceMount": "type=bind,source=${localWorkspaceFolder},target=/workspace/iris",
  "mounts": [
    "type=bind,source=${localWorkspaceFolder}/..,target=/workspace/host"
  ],
  "workspaceFolder": "/workspace/iris",
  "postCreateCommand": "bash -lc 'source /ffm/ffmlite_env.sh && git config --global --add safe.directory /workspace/iris && pip install -e . && grep -qxF \"source /ffm/roccap_env.sh\" ~/.bashrc || echo \"source /ffm/roccap_env.sh\" >> ~/.bashrc'"
}
